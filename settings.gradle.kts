pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "KMMLearning"
include(":androidApp")
include(":shared")
include(":webApp")
